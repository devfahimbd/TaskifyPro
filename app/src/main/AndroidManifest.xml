<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================
         PERMISSIONS
         ============================================================ -->

    <!-- Internet for Firebase communication -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notification permission (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Schedule exact alarms (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Receive boot completed to reschedule alarms -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Wake up the device for the alarm -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Vibrate for notifications -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- ============================================================
         APPLICATION
         ============================================================ -->

    <application
        android:name=".TaskifyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskifyPro"
        tools:targetApi="34">

        <!-- Splash screen meta-data -->
        <meta-data
            android:name="android.window SplashScreen"
            android:resource="@style/Theme.TaskifyPro.Splash" />

        <!-- ========================================================
             ACTIVITIES
             ======================================================== -->

        <!-- Login (launcher) -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.TaskifyPro"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Sign Up -->
        <activity
            android:name=".ui.auth.SignUpActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".ui.auth.LoginActivity" />

        <!-- Main Task List -->
        <activity
            android:name=".ui.task.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Add / Edit Task -->
        <activity
            android:name=".ui.task.AddEditTaskActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".ui.task.MainActivity" />

        <!-- ========================================================
             BROADCAST RECEIVERS
         ======================================================== -->

        <!-- Fires when the device finishes booting -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- Some HTC / Xiaomi devices -->
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Notification alarm receiver -->
        <receiver
            android:name=".receiver.NotificationReceiver"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
